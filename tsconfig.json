{
  "compilerOptions": {
    "target": "ES6",                    /* Output modern JavaScript. */
    "module": "ES6",                    /* Use ES6 modules. */
    "rootDir": "./src",                 /* Specify the source folder for TypeScript files. */
    "outDir": "./dist",                 /* Specify the output folder for JavaScript files. */
    "strict": true,                     /* Enable strict type-checking. */
    "esModuleInterop": true,            /* Allow compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true, /* Enforce consistent file naming. */
    "skipLibCheck": true                /* Skip checking declaration files in libraries. */
  },
  "include": ["src/**/*"],              /* Include all files in the `src` folder. */
  "exclude": ["node_modules"]           /* Exclude the `node_modules` folder. */
}
